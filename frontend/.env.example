# Local dev: leave unset so the app uses same-origin /api URLs via the
# Vite dev-server proxy (vite.config.ts -> http://localhost:8000).
# Set ONLY for builds that must call the API cross-origin directly
# (then the backend must allow CORS for the frontend origin).
# VITE_API_BASE_URL=http://localhost:8000
